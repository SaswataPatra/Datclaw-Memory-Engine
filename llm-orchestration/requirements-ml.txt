# Datclaw Memory Engine - ML & NLP Dependencies (Optional)
#
# Install these ON TOP of requirements.txt for ML-based scoring,
# local classification, and NLP entity extraction.
#
#   pip install -r requirements.txt
#   pip install -r requirements-ml.txt
#   python -m spacy download en_core_web_sm
#
# These add ~2-3 GB of dependencies (PyTorch, Transformers, etc.)
# The core engine runs fine without them using LLM-based alternatives.

# NLP (entity extraction from queries)
spacy>=3.8.0

# Local ML classifiers
torch>=2.2.0
transformers>=4.36.2
sentence-transformers>=2.3.1

# Ego scoring ML combiner
lightgbm>=4.2.0
scikit-learn>=1.4.0
pandas>=2.2.0
shap>=0.44.0

# Training utilities
numba>=0.59.0
matplotlib>=3.8.2
cloudpickle>=3.0.0

# S3 Storage (Cold Archive - only needed for tiered storage)
boto3>=1.34.0
